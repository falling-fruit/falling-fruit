<h2><%= I18n.t("devise.fallingfruit.send_password_instructions") %></h2>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label :email, I18n.t("devise.fallingfruit.email") %><br />
  <%= f.email_field :email %></div>

  <div><%= f.submit I18n.t("devise.fallingfruit.send") %></div>
<% end %>

<%= render "devise/shared/links" %>
